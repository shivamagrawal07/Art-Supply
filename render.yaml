services:
  - type: web
    name: art-supply-backend
    env: node
    rootDir: backend
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 5000
      - key: MONGO_URI
        sync: false # You must set this in the Render dashboard manually!
      - key: JWT_SECRET
        generateValue: true

  - type: web
    name: art-supply-frontend
    env: static
    rootDir: frontend
    buildCommand: npm install && npm run build
    staticPublishPath: dist
    envVars:
      - key: VITE_API_URL
        fromService:
          type: web
          name: art-supply-backend
          envVarKey: RENDER_EXTERNAL_URL
